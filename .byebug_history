c
    @json_map.to_json
      @json_map << {lat:@entries.first.latitude,lng: @entries.first.longitude}
      @json_map << {lat: entries.first.latitude,lng: entries.first.longitude}
      @json_map << {lat: e.latitude,lng: e.longitude}
    @json_map=[]
    end
      @json_map << {lat: e.latitude,lng: e.longitude}
    @entries.each do |e|
 @json_map=[]
@entries = Category.find(params[:category]).entries
