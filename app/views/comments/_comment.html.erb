<%=comment.comment%>
<%= comment.user.email%>
<%#= link_to "Delete Reply" ,[comment.post,comment], method:  :delete, data: { "Are you sure?"}%>
<% if user_signed_in? && current_user.id == comment.user.id%>
<%= link_to "Edit Reply" ,edit_forum_post_comment_path(comment.forum_post,comment)%></br>
<%end%>